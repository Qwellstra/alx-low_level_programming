#include "main.h"

/**
 * _isdigit - checks for digits(ranging from 0-9)
 * @c: the integer to be checked
 * Return: 1 if c is digit, 0 if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
