#include "main.h"
/**
 * _islower - checks whether a string has lower case characters
 * @c: The character to check
 * Return: 1 if c is lower case and 0 if otherwise
 *
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
