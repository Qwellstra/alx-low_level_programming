#include <stdio.h>
/**
 * main - Entry point of the the function
 *
 * Return: 0 if successful, 1 if otherwise
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
