#include <stdio.h>
/**
 * main - Entry point of a function
 *
 * Return: 0 indicates success, 1 indicates otherwise
 */
int main(void)
{
	char c;

	char d;

	c = 'a';
	d = 'A';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(d <= 'Z') {
			putchar(c);
			d++;
		}
	return (0);
}
