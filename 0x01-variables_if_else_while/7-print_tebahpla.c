#include <stdio.h>
/**
 * main - entry point to the main function
 *
 * Return: 0 for success
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c >= 'a') {
			putchar(c);
			c--;
		}
	putchar('\n');
	return (0);
}
