#include <stdio.h>
/**
 * main - Entry into the main function of the file
 *
 * Return: 0 for success
 */

int main(void)
{
	int c;

	c = 0;
	while
		(c < 10) {
			putchar(c + '0');
			c++;
		}
	putchar('\n');
	return (0);
}
